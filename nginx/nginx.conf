worker_processes auto;
error_log /var/log/nginx/error.log warn;
pid /var/run/nginx.pid;

events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;
    sendfile        on;
    tcp_nopush      on;
    keepalive_timeout  65;
    # Character posts two files at once, each up to MAX_UPLOAD_MB (default 40),
    # so the request body ceiling has to clear 2x that plus multipart overhead.
    client_max_body_size 100m;
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml image/svg+xml;
    gzip_min_length 1024;
    # Steam catalog pages request dozens of the same CDN assets. Cache them at
    # the edge so concurrent visitors do not make Python download identical
    # images over and over. Cache is disposable and rebuilt automatically.
    proxy_cache_path /var/cache/nginx/steam levels=1:2 keys_zone=steam_assets:24m
                     max_size=768m inactive=7d use_temp_path=off;

    # security headers
    add_header X-Content-Type-Options nosniff always;
    add_header X-Frame-Options SAMEORIGIN always;
    add_header Referrer-Policy strict-origin-when-cross-origin always;

    upstream app_upstream {
        server app:8080;
        keepalive 16;
    }

    server {
        listen 80;
        server_name _;

        # Immutable-ish static (short cache; put long cache behind Cloudflare with versioned URLs)
        location /static/ {
            alias /var/www/static/;
            expires 7d;
            add_header Cache-Control "public";
            try_files $uri @app;
        }

        location /fonts/ {
            proxy_pass http://app_upstream;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            expires 30d;
        }

        location = /api/steam/proxy-image {
            proxy_pass http://app_upstream;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto https;
            proxy_set_header Connection "";
            proxy_cache steam_assets;
            proxy_cache_lock on;
            proxy_cache_valid 200 7d;
            proxy_cache_valid any 30s;
            proxy_hide_header Cache-Control;
            add_header Cache-Control "public, max-age=86400" always;
            add_header X-Asset-Cache $upstream_cache_status always;
        }

        location = /api/steam/backgrounds {
            proxy_pass http://app_upstream;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto https;
            proxy_set_header Connection "";
            proxy_cache steam_assets;
            proxy_cache_lock on;
            proxy_cache_valid 200 5m;
            add_header X-Asset-Cache $upstream_cache_status always;
        }

        # Never cache API
        location /api/ {
            proxy_pass http://app_upstream;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto https;
            proxy_set_header Connection "";
            proxy_read_timeout 300s;
            proxy_send_timeout 300s;
            proxy_buffering off;
            add_header Cache-Control "no-store" always;
        }

        location / {
            proxy_pass http://app_upstream;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto https;
            proxy_set_header Connection "";
            proxy_read_timeout 120s;
        }

        location @app {
            proxy_pass http://app_upstream;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto https;
        }
    }
}
